

{% block body %}
    <div class="card-body">
        <div class="row">
            <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">


                <h1>Gallery</h1>

                <table class="table">
                    <tbody>
                    <tr>
                        <th>Id</th>
                        <td> {{ image.id }} </td>
                    </tr>
                    <tr>
                        <th>Title</th>
                        <td>{{ image.title }}</td>
                    </tr>
                    <tr>
                        <th>Image</th>
                        <td>{{ image.image }}</td>
                    </tr>
                    </tbody>
                </table>
                <div class="card-body border-top">
                    <a href="{{ path('admin_image_index') }}" <span class="btn btn-success">back to list</span> </a>

                    <a href="{{ path('admin_image_edit', {'id': image.id}) }}"<span class="btn btn-secondary">edit</span></a>
                </div>




            </div>
        </div>
    </div>

{% endblock %}

